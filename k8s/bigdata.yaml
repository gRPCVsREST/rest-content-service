apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: rest-bigdata-content-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: rest-bigdata-content
    spec:
      containers:
        - name: rest-content-service
          image: gcr.io/alien-fold-180922/restcontentservice:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: CONTENT_RESOURCE
              value: "bigdata.txt"
            - name: spring_application_name
              value: "rest-bigdata-content"
---
apiVersion: v1
kind: Service
metadata:
  name: rest-bigdata-content-service
spec:
  type: NodePort
  selector:
    app: rest-bigdata-content
  ports:
   - tagetPort: 8080
     protocol: TCP
---
